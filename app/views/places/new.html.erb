<%= simple_form_for @place do |f| %>
  <%= f.input  :name, placeholder: 'What is its name' %>
  <%= f.input  :address, placeholder: 'Enter the address' %>
  <%= f.input  :content, placeholder: 'Add a description'  %>
  <%= f.button :submit %>
<% end %>
